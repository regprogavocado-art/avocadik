# Avocado — дизайн-система

Источник истины для мини-сайта: `site/src/styles/site.css`, компоненты в
`site/src/components/`. Исходный лендинг сохранён до переноса домена.
Направление — фотореалистичный продукт в спокойном ночном пейзаже, просторная
типографика и тонкие стеклянные поверхности. Без киберпанка и выдуманных метрик.

## Цвет

| Токен | Значение | Назначение |
|---|---|---|
| --bg | #080a09 | Обсидиановый фон |
| --surface | #0f1412 | Поверхности и карточки |
| --surface-2 | #131a16 | Дополнительная поверхность |
| --text | #eef2ee | Основной текст |
| --muted | #a2ada5 | Вторичный текст |
| --muted-2 | #8e9991 | Подписи |
| --mint | #8ff0a6 | Основной акцент, CTA и фокус |
| --on-mint | #06110a | Текст на мятной кнопке |
| --gold | #e3b45c | Косточка, линии и небольшие акценты |
| --line | rgba(221,238,226,.12) | Разделители |

Для текста нужен контраст не ниже 4.5:1. Фотографии затемняются масками;
важный текст не размещается поверх яркой косточки или золотой дороги.

## Сетка и типографика

Контейнер до 1240 px; на широком экране суммарный минимальный отступ 96 px,
на телефоне — 20 px с каждой стороны. Вертикальные секции: 96 px на desktop,
57 px на mobile. Основные границы переключения: 1200, 1080, 760 px.

Локальные Manrope (заголовки), Inter (текст), JetBrains Mono (короткие подписи).
Fallback с подогнанными метриками уменьшает скачки. Заголовок главной до 66 px,
других страниц до 76 px; на телефоне обычно 39–43 px. Текст 16 px / 1.65,
вторичный 14–15 px. Русский sentence case, содержательные заголовки без CAPS.

## Страницы и компоненты

Главная: сцена справа от текста → Hass/Ettinger → каталог строками → аренда →
процесс оплаты → новости при наличии → CTA и подвал. Категории получают собственные
рендеры по `render-brief.md`; пока оригиналов нет, пустые изображения не показываются.

Публичные карточки — мягкий градиент, тонкая граница и радиус 12–16 px.
Стеклянная табличка в hero: тёмный прозрачный фон, blur 8 px, золотистая граница.
Каталог — широкие строки с описанием, статусом и стрелкой; на телефоне содержимое
перестраивается вертикально. Статус «скоро» приходит из CMS, цена может быть «по запросу».

CTA 54 px, навигация и другие тапы минимум 44 px. Коммерческие CTA открывают
существующий чат с контекстом; обычные навигационные ссылки открывают страницы.
На телефоне меню имеет aria-expanded, закрывается Escape и возвращает фокус.
Виджет чата сохраняет историю и API-контракт исходного сайта.

Админка сохраняет палитру, но приоритет — читаемые формы: явные подписи,
сообщения результата, предсказуемые кнопки сохранения. Формы работают без JS.
Счета показывают точную сумму, сеть и неизменяемые реквизиты.

## Изображения и загрузка

Hero — золотая косточка у озера; аренда — дорога к луне; CTA — тёмная панорама.
Маски растворяют края в фоне. Прозрачное авокадо используется на продуктовых
страницах и в OG 1200×630. Оригиналы PNG хранятся вне Git в assets/renders;
публичные WebP и мобильные варианты — в assets/img. Атрибуты размеров и srcset
обязательны. Приоритет высокий только у первого важного изображения страницы.

CSS первого экрана встроен в HTML, локальные шрифты используются без внешних
запросов. Прочие изображения загружаются лениво. Важный текст виден без JS.

## Доступность и движение

Иконки только SVG. Видимый фокус 2 px mint с отступом, skip link, один h1,
последовательные заголовки, явные подписи полей и сообщения ошибок.
Hover 180 ms, движение только по необходимости; prefers-reduced-motion отключает
анимацию и плавный скролл. Изображения-декорации имеют пустой alt.

При приёмке: скриншоты каждой страницы 1440/390, отсутствие горизонтального
скролла и ошибок консоли, axe WCAG AA, меню и чат с клавиатуры. Lighthouse mobile
Performance и Accessibility не ниже 90; отчёт всегда указывает URL и дату замера.

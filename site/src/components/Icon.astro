---
interface Props { name: string; class?: string; }
const { name, class: className = '' } = Astro.props;
---
<svg class:list={['ic', className]} width="24" height="24" aria-hidden="true"><use href={`#i-${name}`} /></svg>
